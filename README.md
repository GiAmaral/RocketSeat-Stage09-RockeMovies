# RocketSeat-Stage09-RockeMovies

To clone and run this project run the following commands:
```bash
git clone git@github.com:GiAmaral/RocketSeat-Stage09-RockeMovies.git rocketMovies
cd rocketMovies
npm install
npm run dev

```
